# Overview

Here you'll set up the tools you'll use for the rest of the course: an accessible,
screen-reader-friendly Python workflow built on **VS Code and scripts**, the **Unix
shell** for navigating files and running commands, and **Git/GitHub** for version control
and assignment submission. (Later in the course, this same VS Code setup connects to the
cloud-based LEAP hub for the data-heavy chapters — but you don't need the cloud yet.)

## Learning objectives

By the end of this module, you should be able to:

1. **Set up and use an accessible working environment** — VS Code with a screen reader, a
   terminal you can read, and Python run from scripts.
2. **Navigate the file system** using shell commands — `pwd`, `ls`, `cd`, and paths
   (relative, absolute, `~`, `.`, `..`).
3. **Create, modify, and remove files and directories** using `mkdir`, `touch`, `mv`,
   `cp`, and `rm` — and understand why "deleted is forever" on the shell.
4. **Explain why version control exists** and what problem it solves.
5. **Track changes with git** — `init`, `add`, `commit`, and inspect history with
   `status` and `log`.
6. **Push code to GitHub** from your environment.
7. **Set up your assignments repository** and submit work using the course's week-folder
   pattern.

Use this list to check your understanding before moving on. If any item feels uncertain,
the page that covers it is linked below.

## Pages in this section

- [Setting up an accessible workflow](./accessible_setup.md) — install VS Code, turn on
  its screen-reader features, and open a terminal you can read
- [Working with Python scripts](./working_with_scripts.md) — how we run Python in this
  course (scripts, not notebooks), and why
- [Intro to Unix](./intro_to_unix.md) — navigating the file system and basic file
  operations
- [Intro to Git](./intro_to_git.md) — version control and the GitHub workflow
- [Assignment 1](./assignment1.md) — set up your assignments repository and submit your
  first week-folder
